#pragma once

#include <stdint.h>
#include "robot_config.h"

typedef struct {
    uint8_t  version;
    uint8_t  source;
    uint16_t type;
    uint16_t sequence;
    uint16_t payload_len;
    uint32_t timestamp_ms;
    uint8_t  payload[ROBOT_MESSAGE_PAYLOAD_MAX];
} robot_message_t;

/* 在此定义命令 ID、载荷布局、字节序、缩放系数和兼容策略。 */
