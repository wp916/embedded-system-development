---
name: embedded-system-development
description: 规划、生成或改造基于 STM32CubeMX、HAL 和 FreeRTOS 的机器人嵌入式控制系统工程，并交付保持 CubeMX 原生目录和工程文件结构的完整项目。适用于硬件方案设计、CubeMX 配置规划、通信增强层、硬件驱动层、应用控制层、系统集成及配套技术文档；默认采用本 Skill 规定的五层逻辑架构，并参考 examples 中的代表性完整工程。不要用于非 STM32 平台或与机器人控制无关的普通 C 工程。
---

An open-source skill by [@wp916](https://github.com/wp916).

# STM32 机器人控制系统开发

## 目标

根据用户给出的机器人功能和硬件条件，形成可实现、可集成、可测试的 STM32 控制系统方案，并按固定逻辑分层生成或改造工程代码。最终交付必须是可由 STM32CubeMX 继续打开和维护、且目录结构与 CubeMX 针对目标 MCU、固件包和工具链生成结果一致的完整工程，而不是仅包含业务源码的代码骨架。

默认技术栈为：

- STM32 系列 MCU；
- STM32CubeMX 生成的 CMSIS、HAL和板级初始化代码；
- FreeRTOS；
- Keil MDK 工具链；
- C 语言；
- 通信增强层、硬件驱动层和应用层组成的模块化控制框架。

如果用户指定了不同的 STM32 型号、IDE、编译器或 FreeRTOS 接口版本，应保留五层架构和接口边界，并对工具链相关部分进行适配。

## 全工程基本规则

开始任何工程规划、代码生成、模块改造或审查前，必须先读取并遵守 `references/project-rules.md`。该文件是所有层级的共同上位约束，规定默认串口1日志资源、延时与执行上下文、HAL 使用边界、资源所有权、幂等初始化和 CubeMX 再生成兼容性。某一层的专用规范只能在不违反这些基本规则的前提下补充细节。

## 固定分层

所有工程在逻辑上按由低到高的顺序组织。以下名称表示职责边界，不要求建立同名顶层目录：

1. 官方底层库（`platform`）：CubeMX 生成的启动文件、CMSIS、HAL、时钟、GPIO、中断、DMA 和外设初始化代码。
2. 实时操作系统层（`rtos`）：FreeRTOS 内核、任务、队列、通知、同步机制、软件定时器及内存配置。
3. 通信增强层（`comms`）：在 HAL 通信接口之上提供非阻塞发送缓冲、连续发送、接收回调绑定、数据分发、负载诊断和错误处理。
4. 硬件驱动层（`drivers`）：封装器件和执行机构，提供独立实例、状态量、控制接口、事件回调和私有运行数据，不承载机器人业务逻辑；详细规范见 `references/drivers.md`。
5. 应用层（`app`）：组合多个硬件实例，实现机器人功能、状态机、控制回路、安全策略、任务编排及对外命令处理。

## CubeMX 原生工程结构

最终工程必须保留目标 STM32CubeMX 版本和所选工具链生成的原生结构。应优先在实际 CubeMX 生成的基础工程上集成代码；不得用自定义的 `platform/`、`rtos/`、`comms/`、`drivers/`、`app/` 顶层目录替代 CubeMX 的标准目录。

使用默认 Keil MDK 工具链和 FreeRTOS 时，完整工程至少应包含与实际配置相符的以下内容：

- `<ProjectName>.ioc`，其 MCU、时钟、引脚、外设、DMA、中断、FreeRTOS 和代码生成设置与源码一致；
- `Core/Inc`、`Core/Src` 和 `Core/Startup`，包含 CubeMX 生成的头文件、初始化源码、中断文件、系统文件和启动文件；
- `Drivers/CMSIS` 和对应系列的 `Drivers/STM32*_HAL_Driver`；
- 启用 FreeRTOS 时包含 `Middlewares/Third_Party/FreeRTOS`，以及 CubeMX 放置在 `Core` 下的 `freertos.c`、`FreeRTOSConfig.h` 等集成文件；
- `MDK-ARM` 下可直接打开和构建的 Keil 工程文件、启动文件引用、链接配置及工程分组；
- 当前 CubeMX 版本实际生成的其他元数据或目录。不要为了凑目录而创建与配置无关的空文件或空文件夹。

通信增强层、硬件驱动层和应用层默认放入 `Core/Inc` 与 `Core/Src`，并在 Keil 工程中按逻辑层建立分组；只有用户明确要求或已有 CubeMX 工程采用独立用户源码目录时，才保留相应扩展目录。所有对 CubeMX 生成文件的修改必须位于 `USER CODE BEGIN/END` 保护区，或放在独立用户文件中，确保重新生成后仍可恢复。

交付前必须将目录树与同一目标 MCU、CubeMX 配置和工具链生成的基准工程逐项核对。至少验证 `.ioc` 可继续用于重新生成、工程引用不存在缺失文件、所有源码和头文件已加入工具链工程、包含路径与宏定义正确。无法实际运行 CubeMX 或编译器时，必须明确标记尚未验证的项目，不得把手工仿制的目录称为已经通过 CubeMX 或编译验证。

## 可复制模板库

`Template/` 保存可直接复制并按目标项目适配的基准工程和代码，目录职责如下：

- `Template/CubeMX/`：按 STM32 芯片或系列分类的 CubeMX 完整工程模板；
- `Template/Comms/UART/`：UART 通信增强层及其发送队列等配套代码；
- `Template/Comms/CAN/`：CAN 总线通信增强层代码；
- `Template/Drivers/YS_Moto/`：YS 电机驱动的头文件和源文件；
- `Template/Drivers/M2006/`：M2006 电机驱动的头文件和源文件；
- `Template/Drivers/` 下的其他目录：传感器、执行器和其他外设的可复用驱动代码。
- `Template/原理图/<开发板>/模块/`：与该开发板配套的显示屏、摄像头等常用模块原理图 PNG；
- `Template/原理图/<开发板>/开发板/`：按功能页拆分的开发板和核心板原理图 PNG。
- `Template/原理图/<开发板>/功能电路/`：从整板原理图中按红色功能框裁出的独立电路 PNG，文件名包含来源页和电路名称。

开始生成完整工程或相关模块前，先检查对应模板目录，只读取与目标 MCU、外设、通信方式和工具链相关的模板。模板应视为只读基准；除非用户明确要求维护模板库，否则将选中的内容复制到目标工程后再修改，不要直接改写 `Template/` 中的原件。

选择 CubeMX 工程模板时，依次核对 MCU 型号或系列、CubeMX 与固件包版本、时钟源、工具链、FreeRTOS 接口和已启用外设。存在多个候选项时，选择约束最接近的模板，并记录从模板复制后修改了哪些配置。没有匹配模板时，按“CubeMX 原生工程结构”创建新工程，不得强行套用不兼容模板。

复制通信层或驱动层代码后，必须适配 HAL 句柄、引脚、外设实例、DMA、中断、缓冲区、RTOS 对象、配置宏和回调绑定，并将源码、头文件、包含路径及必要宏加入目标工具链工程。不得把模板中的板级资源映射、地址、设备数量、控制参数或调试配置直接当作目标工程事实。

工程开始阶段涉及板卡选择、器件选型或接线规划时，检查 `Template/原理图/` 中与候选开发板和器件相关的图片，并向用户提供少量最相关的代表性原理图，帮助确认供电、逻辑电平、接口、必要外围电路和占用的 MCU 资源。不要一次展示整个图片库，也不要因为模板中存在某张图就推断用户已经采用对应硬件。

使用模板原理图前，必须核对开发板版本、器件型号、引脚定义、供电电压和接口标准。原理图图片用于需求沟通与连接参考，实际工程配置仍以用户确认的硬件资料、原厂数据手册和目标板版本为准。

如果模板库没有合适图片，可以在器件型号、电气约束和接口定义已有可靠依据时绘制代表性连接示意图。AI 绘制的图片必须在文件名和图中显著标注“AI 绘制示意图”，同时列出仍需用户或硬件工程师确认的引脚、电压、保护和外围参数；不得把示意图描述为经过原厂或实板验证的生产原理图。

## 示例工程库

`examples/` 用于保存多个按照本 Skill 架构实现的完整工程示例。示例工程用于展示经过实践的工程组织、模块封装、层间组合、通信集成和控制逻辑，不指定任何单一工程作为全部设计的固定基准。

开始生成或改造工程前，先查看 `examples/` 中已有项目，根据 MCU 系列、外设类型、通信方式、硬件驱动和应用结构选择最相关的一个或多个示例。只读取与当前任务相关的文件，不要无目的加载整个示例库：

- CubeMX、HAL 和 FreeRTOS 集成：读取示例中的工程配置、`main.c`、`freertos.c` 和相关外设初始化文件；
- 通信增强层：优先使用 `Template/Comms/` 中的公共实现，示例工程只用于核对实际集成方式；
- 硬件驱动层：选择使用相同器件、相似控制对象或相同通信接口的驱动模块；
- 应用层：选择具有相似状态机、控制流程、硬件组合方式和安全需求的应用模块；
- 模块封装：重点比较公开结构体、私有结构体指针、`fatherArgs`、函数指针回调、软定时器和硬件实例组合方式。

示例工程中稳定的架构模式和已验证公共机制可以复用；MCU 型号、引脚、外设实例、设备数量、内存地址、控制参数、队列容量和调试宏属于具体项目配置，不得机械复制。单个示例中的已知问题、临时代码和项目特例不得自动提升为通用规则。

部分示例源码可能包含非 UTF-8 中文注释。读取时识别并使用其原始编码；除非用户要求，不要改写示例工程的文件编码。

## 必要输入

开始工作时收集并记录：

1. 工程位置以及新建、续写或改造方式；
2. 机器人功能、工作流程、控制目标和验收条件；
3. MCU、板卡、电源、传感器、执行器和通信接口；
4. 实时性要求，包括任务周期、控制频率、通信频率和允许延迟；
5. 已有 CubeMX/Keil 工程、原理图、引脚表、协议、源码和器件手册。

缺失信息应标记为 `TBD`，并说明它会影响的设计或代码。不得猜测器件电气限制、寄存器含义、通信协议、安全阈值或硬件资源映射。

## 问询式协作

整机或完整系统需求采用分阶段问询，以交付完整工程为目标：

- 将信息归为“已确认、待确认、暂定”，每轮只询问当前会阻碍设计或实现的少量关键问题，并在答复后同步更新相关配置和文件。
- 对尚未选定的方案给出 2～4 个适用选项，简述取舍并在依据充分时给出推荐；用户接受推荐后方可视为已确认。用户要求 AI 代选时，记录选择依据和待上板验证风险。
- 不得猜测 MCU/板卡、器件、电气限制、通信协议、引脚与外设映射或安全阈值。此类信息影响正确性时，暂停对应实现并向用户确认。
- 信息未完整时，可先建立不依赖未决项的目录、架构、接口、配置占位和测试框架；受影响内容标记为 `TBD`。关键输入充分后，继续完成剩余工程，不再询问无关偏好。

候选方案应围绕当前项目动态筛选，例如 MCU、板卡、器件、通信接口、FreeRTOS 接口、工具链、控制周期和日志方式。

## 工作流程

1. 确定工程创建地址，并按“问询式协作”，通过多轮沟通，确定工程的主要任务。
2. 创建cubemx工程，生成 `.ioc`文件、并给出cubemx里主要的配置。由用户检查调整完后，你和用户检查沟通，确认最终的 `.ioc`文件，生成基础工程文件。
3. 读取 `references/project-rules.md`，按“问询式协作”整理需求、已知条件、待确认项和验收标准；关键输入不足时先询问，并仅创建不依赖未决信息的工程内容。
4. 分析硬件资源，形成 MCU、器件、接口、引脚、定时器、DMA、中断和内存分配方案。
5. 规划五层架构、模块实例、依赖关系、任务、队列、回调、控制周期、数据流和故障传播路径。
6. 读取 `references/comms.md`，优先从 `Template/Comms/` 复制并适配通信增强层，不满足要求，再重新生成。
7. 读取 `references/drivers.md`，优先从 `Template/Drivers/` 选择匹配实现，并依据器件手册和相关示例工程的稳定模式完成适配，实现初始化、非阻塞控制、状态更新、错误上报和事件回调。
8. 组合硬件实例生成应用层，实现状态机、控制策略、安全逻辑、命令处理和 FreeRTOS 任务装配。
9. 检查分层依赖、接口上下文、缓冲区所有权、ISR 安全性、内存占用、实时性和 CubeMX 再生成兼容性；再按“CubeMX 原生工程结构”逐项检查目录、`.ioc`、工具链工程引用、包含路径和宏定义，并输出文档和测试方案。

## 代码生成原则

- 优先复用公共代码库和示例工程中已经验证的稳定实现，保留其外部行为和关键接口语义。
- 新代码应遵循工程已有的命名、头文件、结构体和接口风格；新建工程应在内部保持一致。
- 代码应包含必要注释，说明模块功能、接口契约、运行上下文、关键参数和使用注意事项；
- 通信增强层和硬件驱动层默认只调用 HAL 公共 API，不主动引入 LL 库，也不直接操作外设寄存器。只有用户明确要求并说明必要性时，才单独评估 LL 或寄存器级实现，同时记录其稳定性、移植性和维护成本。
- 应用层不得直接访问 HAL、LL 或外设寄存器；硬件驱动层不得包含机器人工作模式或任务编排。
- 明确区分阻塞式和异步通信接口：`FOS_Xxx_Transmit(..., Timeout)` 这类带超时参数的阻塞式接口，可以在调用任务中通过 `osDelay(1)` 等待通信资源，使该任务阻塞并让出 CPU；不得使用忙循环或 `HAL_Delay()`。`FOS_Xxx_Transmit_IT/DMA` 等异步接口仍采用提交即返回、链表排队和完成回调推进的模型。
- ISR 和 HAL 中断回调只完成必要的数据搬运、状态记录和任务通知，不执行阻塞操作或复杂应用逻辑。
- 跨任务或 DMA 边界的数据必须明确缓冲区所有权、生命周期、容量和溢出策略。
- 修改 CubeMX 生成文件时必须保持再次生成工程后的可恢复性。
- 未经目标硬件验证的结果只能表述为静态检查、仿真结果或待上板验证，不得宣称已经通过硬件验证。

## 输出

根据任务范围交付以下内容中的必要部分：

- 与目标 CubeMX 配置和工具链生成结果一致的完整工程目录，包括 `.ioc`、`Core`、`Drivers`、所需 `Middlewares` 和工具链工程文件；
- 需求、假设和待确认项；
- 硬件方案、BOM、引脚和外设资源分配；
- 分层架构、模块依赖、任务和数据流设计；
- CubeMX 和 Keil MDK 配置说明；
- 通信增强层、硬件驱动层和应用层代码；
- 公共 API、协议、状态机和内存说明；
- 基准代码复用与适配记录；
- 编译、静态检查和测试结果；
- 尚未解决的风险以及目标板验证步骤。

用户要求完整工程时，最终结果必须满足“CubeMX 原生工程结构”中的全部适用验收项。用户只要求某个模块时，不要擅自生成或改造整个工程，但应说明该模块应放入 CubeMX 工程的具体位置及需要更新的工程引用。
